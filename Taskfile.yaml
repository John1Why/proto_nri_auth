version: "3"

tasks:
  generate:
    aliases:
      - gen
    desc: "Для быстрой генерации proto файлов"
    cmds:
      - protoc -I api/proto api/proto/auth/auth.proto --go_out=api/gen/go --go_opt=paths=source_relative --go-grpc_out=api/gen/go --go-grpc_opt=paths=source_relative